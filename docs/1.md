Технический долг (зафиксирован в AGENTS.md):

1. Text markers — TextSelectionMarkerBehavior не используется, пустой `<Canvas/>` внутри DataTemplate текста, маркеры рендерятся в отдельном ItemsControl вместо внутри DataTemplate.

2. TabItemMiddleClickBehavior / PreviewLineChangedBehavior — не покрыты интеграционными тестами (требуют STA-инфраструктуру).
Отложенные P2-замечания (не критичны, но值得关注):

3. Хрупкость Clone() (P2) — нет теста «все public properties равны кроме Id» для защиты от регресса при добавлении свойств.
4. Неоднородность категорий команд (P2) — MoveObjectCommand / RotateObjectCommand как специализированные классы + ChangePropertyCommand<T> + BatchCommand — нет единого принципа.
5. MainViewModel — 12 зависимостей через DI (строка 19–31), потенциальный god-class по мере роста Этапа 2.

Отложенные фичи:

FR-021 Drag&Drop из библиотеки

FR-022 Preview шаблонов