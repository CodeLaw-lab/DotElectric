# Sprint 25: Рефакторинг моделей — Разделение модели от WPF

**Дата:** 21.04.2026
**Статус:** ✅ ЗАВЕРШЁН
**Тесты:** 1254, 0 сбоев

---

## Цель

Устранить WPF-зависимости из доменной модели, чтобы она могла использоваться в non-WPF контексте (backend, тесты без UI).

## Достигнуто

### Этап 1: Инфраструктура
- ✅ Создан `ObjectBase` — базовый класс БЕЗ INotifyPropertyChanged
- ✅ `ITemplateObject` переработан как абстрактный класс

### Этап 2: Рефакторинг моделей
- ✅ `Line.cs` — убран `ObservableObject`, добавлен конструктор по умолчанию
- ✅ `Text.cs` — убран `ObservableObject`, валидация в сеттере
- ✅ `Rectangle.cs` — убран `ObservableObject`, добавлен конструктор по умолчанию
- ✅ `Sheet.cs`, `GridSettings.cs`, `Metadata.cs` — переработаны без ObservableObject

### Этап 3: Исправления после рефакторинга

#### Этап 2.1: Исправление конструкторов (Id)
- Убран `new string Id` из Line/Text/Rectangle (скрывал базовое свойство)
- Добавлены конструкторы по умолчанию с `Id = Guid.NewGuid()`
- Параметризованные конструкторы вызывают `: this()`

#### Этап 2.2: Исправление MicronsX/MicronsY
- Убран `new long MicronsX/Y` из Text и Rectangle (скрывал базовое свойство)
- Теперь используется базовое свойство `ITemplateObject.MicronsX/Y`

#### Этап 2.3: Восстановление валидации RotationAngle
- Добавлен private field `_rotationAngle` 
- Сеттер с валидацией: `if (!ValidRotationAngles.Contains(value)) throw...`
- Конструктор присваивает через сеттер (валидация)

#### Этап 2.4: Проверка Clone()
- Line, Text, Rectangle переопределяют `Clone()` корректно

### Тесты
- ✅ Обновлён `SetId` в тестах (работа с auto-property)
- ✅ Обновлён `TestTemplateObject` (убраны `new` ключевые слова)
- ✅ Убрано неиспользуемое событие `PropertyChanged`

## Архитектура после рефакторинга

```
Models (БЕЗ WPF)
├── ObjectBase (базовый класс)
├── ITemplateObject (абстрактный класс)
├── Line, Text, Rectangle (конкретные объекты)
└── Sheet, GridSettings, Metadata (вспомогательные)

ViewModels (WPF layer)
├── EditorViewModel
├── PropertiesViewModel
└── ObservableModelWrapper (ещё не требуется)
```

## Изменённые файлы

| Файл | Изменение |
|------|----------|
| `Models/ObjectBase.cs` | `Id: init;` → `Id: set;` |
| `Models/Objects/Line.cs` | Убран `new Id`, добавлен конструктор по умолчанию |
| `Models/Objects/Text.cs` | Убран `new Id/MicronsX/MicronsY`, валидация в сеттере |
| `Models/Objects/Rectangle.cs` | Убран `new Id/MicronsX/MicronsY`, добавлен конструктор |
| `Tests/AdditionalHelperTests.cs` | Исправлен `SetId` |
| `Tests/IntegrationTests.cs` | Исправлен `TestTemplateObject` |
| `Tests/ValidationServiceTests.cs` | Исправлен `TestTemplateObject` |

## Результаты

- **Сборка:** 0 ошибок, 0 предупреждений
- **Тесты:** 1254 проходят, 0 сбоев
- **Модели:** НЕ содержат ссылок на System.ComponentModel

## Критерии завершения

- [x] Модель (Models/*) НЕ содержит ссылок на System.ComponentModel
- [x] Тесты работают без WPF Dispatcher
- [x] Приложение запускается и функционирует корректно
- [x] Сборка проходит без warnings CS0114, CS8618

---

**Автор:** Kilo AI
**Дата создания:** 21.04.2026
