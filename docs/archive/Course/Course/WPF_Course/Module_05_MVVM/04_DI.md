# Тема 5.4: Dependency Injection в WPF

### Теория

**Dependency Injection (DI)** — паттерн внедрения зависимостей через контейнер.

#### Преимущества DI

✅ **Тестируемость** — легко мокировать зависимости  
✅ **Слабая связанность** — компоненты не зависят от конкретных реализаций  
✅ **Гибкость** — легко менять реализации  
✅ **Читаемость** — зависимости явные  

#### Microsoft.Extensions.DependencyInjection

```
Install-Package Microsoft.Extensions.DependencyInjection
Install-Package Microsoft.Extensions.Hosting
```

### Примеры кода

#### Пример 1: Базовая настройка DI в App.xaml.cs

```csharp
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;

public partial class App : Application
{
    private readonly IHost _host;
    private readonly IServiceProvider _serviceProvider;

    public App()
    {
        _host = Host.CreateDefaultBuilder()
            .ConfigureServices((context, services) =>
            {
                // Регистрация сервисов
                services.AddSingleton<ITemplateService, TemplateService>();
                services.AddSingleton<IFileService, FileService>();
                services.AddSingleton<IPrintService, PrintService>();
                services.AddSingleton<IDialogService, DialogService>();
                
                // Регистрация ViewModel
                services.AddSingleton<MainViewModel>();
                services.AddTransient<EditorViewModel>();
                
                // Factory для создания EditorViewModel
                services.AddSingleton<IEditorViewModelFactory, EditorViewModelFactory>();
            })
            .Build();

        _serviceProvider = _host.Services;
    }

    protected override async void OnStartup(StartupEventArgs e)
    {
        base.OnStartup(e);

        // Запуск приложения
        var mainWindow = _serviceProvider.GetRequiredService<MainWindow>();
        mainWindow.Show();

        await _host.StartAsync();
    }

    protected override async void OnExit(ExitEventArgs e)
    {
        await _host.StopAsync();
        _host.Dispose();
        base.OnExit(e);
    }
}
```

#### Пример 2: Сервисы и интерфейсы

```csharp
// ITemplateService.cs
public interface ITemplateService
{
    Template CreateNew();
    Template Load(string path);
    void Save(Template template, string path);
}

// TemplateService.cs
public class TemplateService : ITemplateService
{
    private readonly ILogger<TemplateService> _logger;

    public TemplateService(ILogger<TemplateService> logger)
    {
        _logger = logger;
    }

    public Template CreateNew()
    {
        _logger.LogDebug("Creating new template");
        return new Template();
    }

    public Template Load(string path)
    {
        _logger.LogInformation($"Loading template from {path}");
        // Load logic
        return new Template();
    }

    public void Save(Template template, string path)
    {
        _logger.LogInformation($"Saving template to {path}");
        // Save logic
    }
}

// IFileService.cs
public interface IFileService
{
    Task<Template> LoadAsync(string path);
    Task SaveAsync(Template template, string path);
    bool Exists(string path);
}

// FileService.cs
public class FileService : IFileService
{
    private readonly ITemplateService _templateService;
    private readonly IDialogService _dialogService;

    public FileService(
        ITemplateService templateService, 
        IDialogService dialogService)
    {
        _templateService = templateService;
        _dialogService = dialogService;
    }

    public async Task<Template> LoadAsync(string path)
    {
        // Load logic
        return await Task.FromResult(new Template());
    }

    public async Task SaveAsync(Template template, string path)
    {
        // Save logic
        await Task.CompletedTask;
    }

    public bool Exists(string path) => File.Exists(path);
}
```

#### Пример 3: ViewModel с DI

```csharp
// MainViewModel.cs
public class MainViewModel : ObservableObject
{
    private readonly ITemplateService _templateService;
    private readonly IFileService _fileService;
    private readonly IEditorViewModelFactory _editorVmFactory;

    [ObservableProperty]
    private ObservableCollection<EditorViewModel> _openedTabs;

    [ObservableProperty]
    private EditorViewModel _selectedTab;

    public MainViewModel(
        ITemplateService templateService,
        IFileService fileService,
        IEditorViewModelFactory editorVmFactory)
    {
        _templateService = templateService;
        _fileService = fileService;
        _editorVmFactory = editorVmFactory;

        OpenedTabs = new ObservableCollection<EditorViewModel>();
    }

    [RelayCommand]
    private async Task NewTabAsync()
    {
        var editorVm = _editorVmFactory.Create();
        editorVm.Template = _templateService.CreateNew();
        OpenedTabs.Add(editorVm);
        SelectedTab = editorVm;
    }

    [RelayCommand]
    private async Task OpenFileAsync()
    {
        var path = _dialogService.ShowOpenFileDialog();
        if (path != null)
        {
            var template = await _fileService.LoadAsync(path);
            var editorVm = _editorVmFactory.Create();
            editorVm.Template = template;
            OpenedTabs.Add(editorVm);
            SelectedTab = editorVm;
        }
    }

    [RelayCommand]
    private void CloseTab(EditorViewModel tab)
    {
        if (tab != null)
        {
            OpenedTabs.Remove(tab);
            tab.Dispose();
        }
    }
}
```

#### Пример 4: ViewModel Factory

```csharp
// IEditorViewModelFactory.cs
public interface IEditorViewModelFactory
{
    EditorViewModel Create();
}

// EditorViewModelFactory.cs
public class EditorViewModelFactory : IEditorViewModelFactory
{
    private readonly IServiceProvider _serviceProvider;

    public EditorViewModelFactory(IServiceProvider serviceProvider)
    {
        _serviceProvider = serviceProvider;
    }

    public EditorViewModel Create()
    {
        // Создание ViewModel с внедрением зависимостей
        return new EditorViewModel(
            _serviceProvider.GetRequiredService<ITemplateService>(),
            _serviceProvider.GetRequiredService<IFileService>(),
            _serviceProvider.GetRequiredService<IPrintService>()
        );
    }
}

// EditorViewModel.cs
public partial class EditorViewModel : ObservableObject, IDisposable
{
    private readonly ITemplateService _templateService;
    private readonly IFileService _fileService;
    private readonly IPrintService _printService;

    public EditorViewModel(
        ITemplateService templateService,
        IFileService fileService,
        IPrintService printService)
    {
        _templateService = templateService;
        _fileService = fileService;
        _printService = printService;
    }

    [RelayCommand]
    private async Task SaveAsync()
    {
        await _fileService.SaveAsync(Template, "path.tdel");
    }

    [RelayCommand]
    private async Task PrintAsync()
    {
        await _printService.PrintAsync(Template);
    }

    public void Dispose()
    {
        // Cleanup
    }
}
```

#### Пример 5: Сервисы с lifetime

```csharp
public class App : Application
{
    private void ConfigureServices(IServiceCollection services)
    {
        // Singleton - один экземпляр на всё приложение
        services.AddSingleton<ITemplateService, TemplateService>();
        services.AddSingleton<SettingsService>();
        
        // Transient - новый экземпляр при каждом запросе
        services.AddTransient<EditorViewModel>();
        services.AddTransient<IDialogService, DialogService>();
        
        // Scoped - один экземпляр в рамках scope (для WPF редко используется)
        services.AddScoped<IUnitOfWork, UnitOfWork>();
    }
}
```

#### Пример 6: Сервис настроек

```csharp
// ISettingsService.cs
public interface ISettingsService
{
    T Get<T>(string key, T defaultValue = default);
    void Set<T>(string key, T value);
    void Save();
    void Load();
}

// SettingsService.cs
public class SettingsService : ISettingsService
{
    private readonly string _settingsPath;
    private readonly Dictionary<string, object> _settings;

    public SettingsService()
    {
        _settingsPath = Path.Combine(
            Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
            "DotElectric",
            "settings.json"
        );
        _settings = new Dictionary<string, object>();
    }

    public T Get<T>(string key, T defaultValue = default)
    {
        if (_settings.TryGetValue(key, out var value))
        {
            return (T)value;
        }
        return defaultValue;
    }

    public void Set<T>(string key, T value)
    {
        _settings[key] = value;
    }

    public void Save()
    {
        var json = JsonSerializer.Serialize(_settings);
        File.WriteAllText(_settingsPath, json);
    }

    public void Load()
    {
        if (File.Exists(_settingsPath))
        {
            var json = File.ReadAllText(_settingsPath);
            var settings = JsonSerializer.Deserialize<Dictionary<string, object>>(json);
            
            if (settings != null)
            {
                _settings.Clear();
                foreach (var kvp in settings)
                {
                    _settings[kvp.Key] = kvp.Value;
                }
            }
        }
    }
}
```

#### Пример 7: Сервис диалогов

```csharp
// IDialogService.cs
public interface IDialogService
{
    string ShowOpenFileDialog(string filter = "All files|*.*");
    string ShowSaveFileDialog(string filter = "All files|*.*");
    MessageBoxResult ShowMessage(string message, string title = "", 
                                  MessageBoxButton buttons = MessageBoxButton.OK,
                                  MessageBoxImage icon = MessageBoxImage.None);
    bool? ShowDialog<T>(T viewModel, string title = "") where T : class;
}

// DialogService.cs
public class DialogService : IDialogService
{
    private readonly IServiceProvider _serviceProvider;

    public DialogService(IServiceProvider serviceProvider)
    {
        _serviceProvider = serviceProvider;
    }

    public string ShowOpenFileDialog(string filter = "All files|*.*")
    {
        var dialog = new OpenFileDialog
        {
            Filter = filter
        };
        
        return dialog.ShowDialog() == true ? dialog.FileName : null;
    }

    public string ShowSaveFileDialog(string filter = "All files|*.*")
    {
        var dialog = new SaveFileDialog
        {
            Filter = filter
        };
        
        return dialog.ShowDialog() == true ? dialog.FileName : null;
    }

    public MessageBoxResult ShowMessage(string message, string title = "",
                                         MessageBoxButton buttons = MessageBoxButton.OK,
                                         MessageBoxImage icon = MessageBoxImage.None)
    {
        return MessageBox.Show(message, title, buttons, icon);
    }

    public bool? ShowDialog<T>(T viewModel, string title = "") where T : class
    {
        // Создание окна для ViewModel
        var window = new Window
        {
            Title = title,
            DataContext = viewModel,
            SizeToContent = SizeToContent.WidthAndHeight,
            WindowStartupLocation = WindowStartupLocation.CenterScreen
        };

        return window.ShowDialog();
    }
}
```

#### Пример 8: Реальная настройка из DotElectric

```csharp
// App.xaml.cs
public partial class App : Application
{
    private readonly IHost _host;
    private readonly Serilog.ILogger _logger;

    public App()
    {
        // Serilog настройка
        Log.Logger = new LoggerConfiguration()
            .MinimumLevel.Debug()
            .WriteTo.File(
                Path.Combine(
                    Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
                    "DotElectric",
                    "logs",
                    "dotelectric-.txt"
                ),
                rollingInterval: RollingInterval.Day,
                retainedFileCountLimit: 30
            )
            .CreateLogger();

        _logger = Log.Logger;

        _host = Host.CreateDefaultBuilder()
            .UseContentRoot(AppContext.BaseDirectory)
            .ConfigureServices((context, services) =>
            {
                // Сервисы
                services.AddSingleton<ITemplateService, TemplateService>();
                services.AddSingleton<IFileService, FileService>();
                services.AddSingleton<IPrintService, PrintService>();
                services.AddSingleton<ISettingsService, SettingsService>();
                services.AddSingleton<IDialogService, DialogService>();
                services.AddSingleton<IThemeService, ThemeService>();
                services.AddSingleton<AutosaveService>();
                
                // Logger
                services.AddSingleton<Serilog.ILogger>(_logger);
                
                // ViewModel
                services.AddSingleton<MainViewModel>();
                services.AddSingleton<IEditorViewModelFactory, EditorViewModelFactory>();
                
                // Dialogs
                services.AddTransient<CustomSheetDialogViewModel>();
            })
            .Build();
    }

    protected override void OnStartup(StartupEventArgs e)
    {
        base.OnStartup(e);

        // Глобальные обработчики исключений
        AppDomain.CurrentDomain.UnhandledException += (s, args) =>
        {
            _logger.Fatal((Exception)args.ExceptionObject, "Unhandled exception");
        };

        DispatcherUnhandledException += (s, args) =>
        {
            _logger.Error(args.Exception, "Dispatcher unhandled exception");
            args.Handled = true;
        };

        // Запуск MainWindow
        var mainWindow = _host.Services.GetRequiredService<MainWindow>();
        mainWindow.DataContext = _host.Services.GetRequiredService<MainViewModel>();
        mainWindow.Show();

        _host.StartAsync();
    }
}
```

---

### Задачи

#### 🟢 Базовый уровень (45 минут)

**Задача 5.4.1: Простой сервис**

Создайте сервис:
- IMessageService с методом Show(string message)
- Реализация через MessageBox
- Регистрация в DI

**Задача 5.4.2: ViewModel с зависимостью**

Создайте ViewModel:
- Зависимость от IMessageService
- Команда ShowMessage
- Отображение сообщения через сервис

---

#### 🟡 Средний уровень (1.5 часа)

**Задача 5.4.3: Settings Service**

Создайте сервис настроек:
- Get/Set методы
- Сохранение в JSON файл
- Загрузка при старте

**Задача 5.4.4: Factory паттерн**

Реализуйте factory:
- IViewModelFactory
- Создание ViewModel с DI
- Регистрация фабрики в контейнере

---

#### 🔴 Продвинутый уровень (2.5 часа)

**Задача 5.4.5: Multi-Service ViewModel**

Создайте ViewModel с несколькими сервисами:
- ITemplateService
- IFileService
- IDialogService
- ISettingsService
- Все сервисы внедряются через конструктор

**Задача 5.4.6: Scoped Services**

Реализуйте scope для временных сервисов:
- IServiceScope для создания scope
- Временные сервисы (например, для диалогов)
- Правильное освобождение ресурсов

---

### Решения

<details>
<summary>✅ Решение задачи 5.4.1</summary>

```csharp
// IMessageService.cs
public interface IMessageService
{
    void Show(string message);
}

// MessageBoxService.cs
public class MessageBoxService : IMessageService
{
    public void Show(string message)
    {
        MessageBox.Show(message, "Message", MessageBoxButton.OK, MessageBoxImage.Information);
    }
}

// App.xaml.cs
services.AddSingleton<IMessageService, MessageBoxService>();

// MainViewModel.cs
public class MainViewModel : ObservableObject
{
    private readonly IMessageService _messageService;

    public MainViewModel(IMessageService messageService)
    {
        _messageService = messageService;
    }

    [RelayCommand]
    private void ShowMessage()
    {
        _messageService.Show("Hello from DI!");
    }
}
```
</details>

<details>
<summary>✅ Решение задачи 5.4.3</summary>

```csharp
public class SettingsService : ISettingsService
{
    private readonly string _path;
    private Dictionary<string, object> _settings;

    public SettingsService()
    {
        _path = Path.Combine(
            Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
            "MyApp",
            "settings.json"
        );
        _settings = new Dictionary<string, object>();
        Load();
    }

    public T Get<T>(string key, T defaultValue = default)
    {
        return _settings.TryGetValue(key, out var value) 
            ? (T)value 
            : defaultValue;
    }

    public void Set<T>(string key, T value)
    {
        _settings[key] = value;
    }

    public void Save()
    {
        Directory.CreateDirectory(Path.GetDirectoryName(_path));
        var json = JsonSerializer.Serialize(_settings);
        File.WriteAllText(_path, json);
    }

    public void Load()
    {
        if (File.Exists(_path))
        {
            var json = File.ReadAllText(_path);
            _settings = JsonSerializer.Deserialize<Dictionary<string, object>>(json) 
                ?? new Dictionary<string, object>();
        }
    }
}
```
</details>

---

## Ключевые выводы

✅ **DI** — внедрение зависимостей через контейнер  
✅ **IServiceCollection** — регистрация сервисов  
✅ **ServiceLifetime**: Singleton, Transient, Scoped  
✅ **Factory** — создание ViewModel с DI  
✅ **Конструктор** — явное указание зависимостей  
✅ **Microsoft.Extensions.Hosting** — хостинг для WPF  
✅ **IServiceProvider** — получение сервисов

---

## Дополнительные ресурсы

- [Dependency Injection](https://docs.microsoft.com/en-us/dotnet/core/extensions/dependency-injection)
- [Microsoft.Extensions.Hosting](https://docs.microsoft.com/en-us/dotnet/core/extensions/generic-host)
- [DI Best Practices](https://docs.microsoft.com/en-us/dotnet/core/extensions/dependency-injection-guidelines)
