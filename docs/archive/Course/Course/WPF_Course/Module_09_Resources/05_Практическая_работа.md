# Модуль 9: Практическая работа

## Задание: Создание системы тем для приложения

**Время выполнения:** 3 часа  
**Уровень:** Продвинутый  
**Максимальный балл:** 100

---

## Описание

Необходимо создать полноценную систему тем для WPF приложения с поддержкой Light/Dark тем и MaterialDesignThemes.

---

## Требования

### 1. ResourceDictionary (30 минут)

Создайте структуру ресурсов:

```
Resources/
├── Themes/
│   ├── LightTheme.xaml
│   └── DarkTheme.xaml
├── Styles/
│   ├── Colors.xaml
│   ├── Buttons.xaml
│   └── TextBoxes.xaml
└── Converters/
    └── Converters.xaml
```

**Colors.xaml:**
- 5+ цветов
- 5+ кистей
- 2+ значения (double, thickness)

---

### 2. LightTheme.xaml (30 минут)

Создайте светлую тему:

```xml
<ResourceDictionary>
    <!-- Цвета -->
    <Color x:Key="WindowBackgroundColor">#FFFFFF</Color>
    <Color x:Key="PanelBackgroundColor">#F5F5F5</Color>
    <Color x:Key="TextPrimaryColor">#212121</Color>
    <Color x:Key="TextSecondaryColor">#757575</Color>
    <Color x:Key="AccentColor">#0078D4</Color>
    
    <!-- Кисти -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    
    <!-- Стили -->
    <Style TargetType="Button">...</Style>
    <Style TargetType="TextBox">...</Style>
</ResourceDictionary>
```

---

### 3. DarkTheme.xaml (30 минут)

Создайте тёмную тему:

```xml
<ResourceDictionary>
    <!-- Цвета -->
    <Color x:Key="WindowBackgroundColor">#212121</Color>
    <Color x:Key="PanelBackgroundColor">#303030</Color>
    <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
    <Color x:Key="TextSecondaryColor">#BDBDBD</Color>
    <Color x:Key="AccentColor">#42A5F5</Color>
    
    <!-- Кисти -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
</ResourceDictionary>
```

---

### 4. ThemeService (45 минут)

Создайте сервис переключения тем:

```csharp
public interface IThemeService
{
    bool IsDarkTheme { get; }
    string CurrentThemeName { get; }
    
    void ToggleTheme();
    void SetDarkTheme();
    void SetLightTheme();
    
    event EventHandler ThemeChanged;
}

public class ThemeService : IThemeService
{
    // Реализация
}
```

**Требования:**
- Переключение через MergedDictionaries
- Событие ThemeChanged
- Сохранение в настройки (опционально)

---

### 5. MainViewModel (30 минут)

Создайте ViewModel:

```csharp
public partial class MainViewModel : ObservableObject
{
    private readonly IThemeService _themeService;

    [ObservableProperty]
    private string _currentThemeName;

    [ObservableProperty]
    private bool _isDarkTheme;

    public ICommand ToggleThemeCommand { get; }

    public MainViewModel(IThemeService themeService)
    {
        _themeService = themeService;
        IsDarkTheme = _themeService.IsDarkTheme;
        CurrentThemeName = _themeService.CurrentThemeName;
        
        ToggleThemeCommand = new RelayCommand(ToggleTheme);
        
        _themeService.ThemeChanged += (s, e) =>
        {
            IsDarkTheme = _themeService.IsDarkTheme;
            CurrentThemeName = _themeService.CurrentThemeName;
        };
    }

    private void ToggleTheme() => _themeService.ToggleTheme();
}
```

---

### 6. MainWindow с MaterialDesign (45 минут)

Создайте главное окно:

```xml
<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <materialDesign:DialogHost>
        <Grid Background="{DynamicResource WindowBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Toolbar -->
            <ToolBar Grid.Row="0">
                <Button Content="🌓 Тема"
                        Command="{Binding ToggleThemeCommand}"
                        Style="{StaticResource MaterialDesignIconButton}"/>
                <TextBlock Text="{Binding CurrentThemeName}"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
            </ToolBar>
            
            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20">
                <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         materialDesign:HintAssist.Hint="Enter text"
                         Margin="0,0,0,20"/>
                
                <Button Content="Primary Button"
                        Style="{StaticResource MaterialDesignRaisedPrimaryButton}"
                        Margin="0,0,0,10"/>
                
                <Button Content="Secondary Button"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
                
                <materialDesign:Card Margin="0,20,0,0">
                    <StackPanel Margin="16">
                        <TextBlock Text="Card Title"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                        <TextBlock Text="Card content"
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </Grid>
    </materialDesign:DialogHost>
</Window>
```

---

## Критерии оценки

| Критерий | Баллы |
|----------|-------|
| **ResourceDictionary** | 15 |
| — Структура папок | 3 |
| — Colors.xaml (5+ цветов) | 4 |
| — Buttons.xaml | 4 |
| — TextBoxes.xaml | 4 |
| **LightTheme** | 15 |
| — 5+ цветов | 5 |
| — 5+ кистей | 5 |
| — Стили контролов | 5 |
| **DarkTheme** | 15 |
| — 5+ цветов | 5 |
| — 5+ кистей | 5 |
| — Стили контролов | 5 |
| **ThemeService** | 20 |
| — IThemeService интерфейс | 5 |
| — ToggleTheme метод | 5 |
| — ThemeChanged event | 5 |
| — MergedDictionaries | 5 |
| **MainViewModel** | 15 |
| — ObservableProperty | 5 |
| — ToggleThemeCommand | 5 |
| — Подписка на ThemeChanged | 5 |
| **MainWindow** | 15 |
| — MaterialDesignThemes | 5 |
| — DynamicResource | 5 |
| — Toolbar с кнопкой темы | 5 |
| **Code Quality** | 5 |
| — Именование | 2 |
| — Организация кода | 2 |
| — Комментарии | 1 |
| **Итого** | **100** |

---

## Дополнительные задания (со звёздочкой)

### ⭐ Settings Service (30 минут)

Реализуйте сохранение темы:
- ISettingsService
- Сохранение IsDarkTheme
- Загрузка при старте

**Бонус:** +10 баллов

### ⭐⭐ Animation (45 минут)

Добавьте анимацию:
- Fade out перед сменой
- Fade in после смены
- Async/await

**Бонус:** +15 баллов

### ⭐⭐⭐ Multiple Themes (2 часа)

Добавьте 3+ темы:
- Light, Dark, Blue, Green
- ComboBox для выбора
- Динамическая загрузка

**Бонус:** +20 баллов

---

## Чек-лист перед сдачей

- [ ] Структура Resources/ папок создана
- [ ] LightTheme.xaml с 5+ цветами
- [ ] DarkTheme.xaml с 5+ цветами
- [ ] ThemeService реализован
- [ ] IThemeService интерфейс
- [ ] ToggleTheme метод работает
- [ ] MainViewModel с Command
- [ ] MainWindow с MaterialDesign
- [ ] DynamicResource для цветов
- [ ] Кнопка переключения темы
- [ ] TextBlock с названием темы
- [ ] Код компилируется без ошибок

---

## Пример решения (фрагменты)

<details>
<summary>✅ ThemeService.cs</summary>

```csharp
public class ThemeService : IThemeService
{
    private readonly ResourceDictionary _lightTheme;
    private readonly ResourceDictionary _darkTheme;
    private bool _isDarkTheme;

    public bool IsDarkTheme => _isDarkTheme;
    public string CurrentThemeName => _isDarkTheme ? "Dark" : "Light";
    
    public event EventHandler ThemeChanged;

    public ThemeService()
    {
        _lightTheme = new ResourceDictionary
        {
            Source = new Uri("Resources/Themes/LightTheme.xaml", UriKind.Relative)
        };

        _darkTheme = new ResourceDictionary
        {
            Source = new Uri("Resources/Themes/DarkTheme.xaml", UriKind.Relative)
        };

        _isDarkTheme = true;
        Application.Current.Resources.MergedDictionaries.Add(_darkTheme);
    }

    public void ToggleTheme()
    {
        if (_isDarkTheme)
        {
            Application.Current.Resources.MergedDictionaries.Remove(_darkTheme);
            Application.Current.Resources.MergedDictionaries.Add(_lightTheme);
        }
        else
        {
            Application.Current.Resources.MergedDictionaries.Remove(_lightTheme);
            Application.Current.Resources.MergedDictionaries.Add(_darkTheme);
        }
        
        _isDarkTheme = !_isDarkTheme;
        ThemeChanged?.Invoke(this, EventArgs.Empty);
    }

    public void SetDarkTheme()
    {
        if (!_isDarkTheme)
        {
            ToggleTheme();
        }
    }

    public void SetLightTheme()
    {
        if (_isDarkTheme)
        {
            ToggleTheme();
        }
    }
}
```
</details>

<details>
<summary>✅ App.xaml</summary>

```xml
<Application x:Class="WpfApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:CustomColorTheme BaseTheme="Dark"
                                                  PrimaryColor="#0078D4"
                                                  SecondaryColor="#66BB6A"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml"/>
                <ResourceDictionary Source="Resources/Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
```
</details>

---

## Ресурсы

- [MaterialDesignThemes](https://github.com/MaterialDesignInXAML/MaterialDesignInXamlToolkit)
- [ResourceDictionary](https://docs.microsoft.com/en-us/dotnet/api/system.windows.resourcedictionary)
- [Theming in WPF](https://docs.microsoft.com/en-us/dotnet/desktop/wpf/fundamentals/styles-templates)
