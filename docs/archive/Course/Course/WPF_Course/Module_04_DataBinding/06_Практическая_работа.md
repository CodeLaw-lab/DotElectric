# Модуль 4: Практическая работа

## Задание: Создание приложения "Список сотрудников"

**Время выполнения:** 4 часа  
**Уровень:** Средний  
**Максимальный балл:** 100

---

## Описание

Необходимо создать WPF-приложение для управления списком сотрудников с использованием Data Binding, конвертеров, валидации и работы с коллекциями.

---

## Требования

### 1. Модель данных (30 минут)

Создайте класс `Employee`:

```csharp
public class Employee
{
    public int Id { get; set; }
    public string FirstName { get; set; }
    public string LastName { get; set; }
    public string Email { get; set; }
    public int Age { get; set; }
    public decimal Salary { get; set; }
    public DateTime HireDate { get; set; }
    public bool IsActive { get; set; }
    public string Department { get; set; }
    public string AvatarUrl { get; set; }
}
```

---

### 2. ViewModel (45 минут)

Создайте `MainViewModel` с:

**Свойства:**
- `Employees` — ObservableCollection&lt;Employee&gt;
- `SelectedEmployee` — выбранный сотрудник
- `SearchText` — текст поиска
- `FilterDepartment` — фильтр по отделу
- `SortBy` — сортировка (по имени, зарплате, дате)

**Команды:**
- `AddCommand` — добавить нового
- `DeleteCommand` — удалить выбранного
- `SaveCommand` — сохранить изменения
- `ClearSearchCommand` — очистить поиск

---

### 3. Конвертеры (45 минут)

Создайте конвертеры:

**BoolToVisibilityConverter:**
- Для отображения статуса (Active/Inactive)

**SalaryToStringConverter:**
- Формат: "$100,000.00"
- ConvertBack для ввода

**DateToStringConverter:**
- Формат: "dd.MM.yyyy"
- Parameter для разных форматов

**EmailToAvatarConverter:**
- Генерация URL аватара по email
- Пример: `https://api.dicebear.com/7.x/initials/svg?seed={email}`

---

### 4. Главное окно (1 час)

Создайте MainWindow с:

```xml
<Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    
    <!-- Toolbar с кнопками -->
    <ToolBar Grid.Row="0">
        <Button Content="➕ Добавить" Command="{Binding AddCommand}"/>
        <Button Content="🗑️ Удалить" Command="{Binding DeleteCommand}"/>
        <Button Content="💾 Сохранить" Command="{Binding SaveCommand}"/>
    </ToolBar>
    
    <!-- Панель фильтров -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
        <TextBox Width="200" 
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Watermark="Поиск..."/>
        <ComboBox Width="150" Margin="10,0,0,0"
                  ItemsSource="{Binding Departments}"
                  SelectedItem="{Binding FilterDepartment}"/>
        <ComboBox Width="150" Margin="10,0,0,0"
                  SelectedItem="{Binding SortBy}">
            <ComboBoxItem Content="По имени"/>
            <ComboBoxItem Content="По зарплате"/>
            <ComboBoxItem Content="По дате найма"/>
        </ComboBox>
    </StackPanel>
    
    <!-- Список сотрудников -->
    <ListBox Grid.Row="2" 
             ItemsSource="{Binding Employees}"
             SelectedItem="{Binding SelectedEmployee}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <!-- Карточка сотрудника -->
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
    
    <!-- Панель редактирования -->
    <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="0,1,0,0">
        <!-- Форма редактирования -->
    </Border>
</Grid>
```

---

### 5. DataTemplate сотрудника (45 минут)

Создайте DataTemplate для отображения:

```xml
<DataTemplate>
    <Border Padding="10" Margin="5" BorderThickness="1" BorderBrush="#E0E0E0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Аватар -->
            <Image Grid.Column="0" Width="60" Height="60">
                <Image.Source>
                    <Binding Path="Email" Converter="{StaticResource EmailToAvatar}"/>
                </Image.Source>
            </Image>
            
            <!-- Информация -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding FirstName}" FontWeight="Bold" FontSize="16"/>
                    <TextBlock Text=" "/>
                    <TextBlock Text="{Binding LastName}" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
                
                <TextBlock Text="{Binding Email}" Foreground="Gray" FontSize="13"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="{Binding Department}"/>
                    <TextBlock Text=" | "/>
                    <TextBlock Text="{Binding Salary, Converter={StaticResource SalaryConverter}}"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Статус -->
            <StackPanel Grid.Column="2">
                <Ellipse Width="15" Height="15" 
                         HorizontalAlignment="Right">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter Property="Fill" Value="#28A745"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                    <Setter Property="Fill" Value="#6C757D"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock FontSize="10" HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter Property="Text" Value="Active"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                    <Setter Property="Text" Value="Inactive"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</DataTemplate>
```

---

### 6. Форма редактирования (45 минут)

Создайте форму для редактирования выбранного сотрудника:

```xml
<Border Grid.Row="3" Padding="20" Visibility="{Binding SelectedEmployee, Converter={StaticResource NullToVisibility}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- First Name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:" Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding SelectedEmployee.FirstName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        
        <!-- Last Name -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Фамилия:" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding SelectedEmployee.LastName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        
        <!-- Email -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" 
                 Text="{Binding SelectedEmployee.Email, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        
        <!-- Age -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Возраст:" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" 
                 Text="{Binding SelectedEmployee.Age, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        
        <!-- Salary -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Зарплата:" Margin="5"/>
        <TextBox Grid.Row="4" Grid.Column="1" 
                 Text="{Binding SelectedEmployee.Salary, Converter={StaticResource SalaryConverter}, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>
        
        <!-- Active -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Активен:" Margin="5"/>
        <CheckBox Grid.Row="5" Grid.Column="1" 
                  IsChecked="{Binding SelectedEmployee.IsActive}"
                  Margin="5"/>
    </Grid>
</Border>
```

---

### 7. Валидация (30 минут)

Реализуйте валидацию для Employee:

```csharp
public class Employee : IDataErrorInfo
{
    // ... свойства ...
    
    public string this[string columnName]
    {
        get
        {
            switch (columnName)
            {
                case "FirstName":
                    if (string.IsNullOrWhiteSpace(FirstName))
                        return "Имя обязательно";
                    if (FirstName.Length < 2)
                        return "Минимум 2 символа";
                    break;
                    
                case "LastName":
                    if (string.IsNullOrWhiteSpace(LastName))
                        return "Фамилия обязательна";
                    break;
                    
                case "Email":
                    if (!Regex.IsMatch(Email, @"^[^@\s]+@[^@\s]+\.[^@\s]+$"))
                        return "Некорректный email";
                    break;
                    
                case "Age":
                    if (Age < 18 || Age > 100)
                        return "Возраст от 18 до 100";
                    break;
                    
                case "Salary":
                    if (Salary < 0)
                        return "Зарплата не может быть отрицательной";
                    break;
            }
            return null;
        }
    }
    
    public string Error => null;
}
```

```xml
<!-- TextBox с валидацией -->
<TextBox>
    <TextBox.Text>
        <Binding Path="SelectedEmployee.FirstName" 
                 UpdateSourceTrigger="PropertyChanged"
                 ValidatesOnDataErrors="True"
                 NotifyOnValidationError="True"/>
    </TextBox.Text>
    <TextBox.Style>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="ToolTip" 
                            Value="{Binding (Validation.Errors)[0].ErrorContent, 
                            RelativeSource={RelativeSource Self}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </TextBox.Style>
</TextBox>
```

---

## Критерии оценки

| Критерий | Баллы |
|----------|-------|
| **Модель данных** | 5 |
| — Все свойства реализованы | 3 |
| — IDataErrorInfo | 2 |
| **ViewModel** | 20 |
| — ObservableCollection | 5 |
| — INotifyPropertyChanged | 5 |
| — Commands (4 шт) | 5 |
| — Фильтры и сортировка | 5 |
| **Конвертеры** | 20 |
| — BoolToVisibility | 5 |
| — SalaryToString | 5 |
| — DateToString | 5 |
| — EmailToAvatar | 5 |
| **UI Layout** | 15 |
| — Toolbar с кнопками | 3 |
| — Панель фильтров | 4 |
| — ListBox с DataTemplate | 5 |
| — Форма редактирования | 3 |
| **DataTemplate** | 15 |
| — Отображение всех полей | 5 |
| — Аватар | 3 |
| — Статус (Active/Inactive) | 4 |
| — Styling | 3 |
| **Валидация** | 15 |
| — FirstName/LastName | 3 |
| — Email | 3 |
| — Age | 3 |
| — Salary | 3 |
| — Визуализация ошибок | 3 |
| **Code Quality** | 10 |
| — Именование | 3 |
| — DRY | 3 |
| — Организация кода | 2 |
| — Комментарии | 2 |
| **Итого** | **100** |

---

## Дополнительные задания (со звёздочкой)

### ⭐ Сортировка (30 минут)

Реализуйте сортировку через ICollectionView:
- Сортировка по имени, зарплате, дате
- Ascending/Descending
- Индикатор текущей сортировки

**Бонус:** +10 баллов

### ⭐⭐ Фильтрация (45 минут)

Реализуйте фильтрацию через ICollectionView:
- Поиск по имени/фамилии/email
- Фильтр по отделу
- Комбинирование фильтров

**Бонус:** +15 баллов

### ⭐⭐⭐ Группировка (1.5 часа)

Добавьте группировку по отделам:
- GroupStyle для ListBox
- Заголовки групп
- Счётчик элементов в группе
- Collapse/Expand группы

**Бонус:** +20 баллов

---

## Пример решения (фрагменты)

<details>
<summary>MainViewModel.cs</summary>

```csharp
public class MainViewModel : INotifyPropertyChanged
{
    private readonly ObservableCollection<Employee> _employees;
    private Employee _selectedEmployee;
    private string _searchText;

    public ObservableCollection<Employee> Employees => _employees;

    public Employee SelectedEmployee
    {
        get => _selectedEmployee;
        set { _selectedEmployee = value; OnPropertyChanged(); }
    }

    public string SearchText
    {
        get => _searchText;
        set 
        { 
            _searchText = value; 
            OnPropertyChanged();
            FilterEmployees();
        }
    }

    public ICommand AddCommand { get; }
    public ICommand DeleteCommand { get; }
    public ICommand SaveCommand { get; }

    public MainViewModel()
    {
        _employees = new ObservableCollection<Employee>();
        
        AddCommand = new RelayCommand(AddEmployee, CanAddEmployee);
        DeleteCommand = new RelayCommand(DeleteEmployee, CanDeleteEmployee);
        SaveCommand = new RelayCommand(SaveEmployees);
        
        // Загрузка тестовых данных
        LoadSampleData();
    }

    private void AddEmployee()
    {
        var employee = new Employee
        {
            Id = _employees.Max(e => e.Id) + 1,
            FirstName = "New",
            LastName = "Employee",
            IsActive = true
        };
        _employees.Add(employee);
        SelectedEmployee = employee;
    }

    private void DeleteEmployee()
    {
        if (SelectedEmployee != null)
        {
            _employees.Remove(SelectedEmployee);
            SelectedEmployee = null;
        }
    }

    private void FilterEmployees()
    {
        // Фильтрация через ICollectionView
        var view = CollectionViewSource.GetDefaultView(_employees);
        view.Filter = e =>
        {
            var emp = (Employee)e;
            if (string.IsNullOrWhiteSpace(SearchText))
                return true;
            
            return emp.FirstName.Contains(SearchText, StringComparison.OrdinalIgnoreCase) ||
                   emp.LastName.Contains(SearchText, StringComparison.OrdinalIgnoreCase) ||
                   emp.Email.Contains(SearchText, StringComparison.OrdinalIgnoreCase);
        };
    }

    public event PropertyChangedEventHandler PropertyChanged;
    protected void OnPropertyChanged([CallerMemberName] string name = null) =>
        PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
}
```
</details>

<details>
<summary>SalaryToStringConverter.cs</summary>

```csharp
public class SalaryToStringConverter : IValueConverter
{
    public object Convert(object value, Type targetType, 
                          object parameter, CultureInfo culture)
    {
        if (value is decimal salary)
        {
            return salary.ToString("C", CultureInfo.GetCultureInfo("en-US"));
        }
        return "0";
    }

    public object ConvertBack(object value, Type targetType, 
                              object parameter, CultureInfo culture)
    {
        if (value is string str)
        {
            str = Regex.Replace(str, @"[^\d.-]", "");
            if (decimal.TryParse(str, out var salary))
            {
                return salary;
            }
        }
        return 0m;
    }
}
```
</details>

---

## Чек-лист перед сдачей

- [ ] Employee класс со всеми свойствами
- [ ] ViewModel с ObservableCollection
- [ ] INotifyPropertyChanged реализован
- [ ] 4 команды (Add, Delete, Save, ClearSearch)
- [ ] 4 конвертера работают корректно
- [ ] ListBox с DataTemplate отображает сотрудников
- [ ] Форма редактирования связана с SelectedEmployee
- [ ] Валидация работает для всех полей
- [ ] Ошибки валидации отображаются (красная рамка, tooltip)
- [ ] Поиск фильтрует коллекцию
- [ ] UI выглядит аккуратно

---

## Ресурсы

- [Data Binding](https://docs.microsoft.com/en-us/dotnet/desktop/wpf/data/data-binding-overview)
- [IValueConverter](https://docs.microsoft.com/en-us/dotnet/api/system.windows.data.ivalueconverter)
- [ObservableCollection](https://docs.microsoft.com/en-us/dotnet/api/system.collections.objectmodel.observablecollection-1)
- [IDataErrorInfo](https://docs.microsoft.com/en-us/dotnet/api/system.componentmodel.idataerrorinfo)
