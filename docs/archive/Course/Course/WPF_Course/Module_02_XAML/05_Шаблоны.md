# Тема 2.5: Шаблоны (ControlTemplate, DataTemplate)

### Теория

**ControlTemplate** — определяет визуальную структуру контрола.  
**DataTemplate** — определяет визуализацию данных.

#### ControlTemplate vs DataTemplate

| Характеристика | ControlTemplate | DataTemplate |
|----------------|-----------------|--------------|
| **Назначение** | Внешний вид контрола | Визуализация данных |
| **TargetType** | Тип контрола (Button) | Тип данных (Person) |
| **TemplateBinding** | Да | Нет |
| **Использование** | Переопределение контрола | ItemsControl, ContentControl |

#### TemplateBinding

```xml
<ControlTemplate TargetType="Button">
    <!-- TemplateBinding передаёт значение свойства из контрола в template -->
    <Border Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"/>
</ControlTemplate>
```

#### TemplatePart атрибут

```csharp
[TemplatePart(Name = "PART_ButtonBorder", Type = typeof(Border))]
[TemplatePart(Name = "PART_ContentHost", Type = typeof(ContentPresenter))]
public class CustomButton : Button
{
    public override void OnApplyTemplate()
    {
        base.OnApplyTemplate();
        var border = GetTemplateChild("PART_ButtonBorder") as Border;
    }
}
```

### Примеры кода

#### Пример 1: Простой ControlTemplate для Button

```xml
<Window.Resources>
    <ControlTemplate x:Key="SimpleButtonTemplate" TargetType="Button">
        <Border x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="5">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
        </Border>
        
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" 
                        Value="{TemplateBinding BorderBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="border" Property="Background" Value="#004080"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</Window.Resources>

<Button Content="Click me"
        Background="#0078D4"
        BorderBrush="#005A9E"
        BorderThickness="2"
        Template="{StaticResource SimpleButtonTemplate}"/>
```

#### Пример 2: Полный ControlTemplate для Button с анимацией

```xml
<ControlTemplate TargetType="Button">
    <Grid>
        <Border x:Name="backgroundBorder"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" 
                                  ShadowDepth="2"
                                  Opacity="0.3"/>
            </Border.Effect>
        </Border>
        
        <!-- Content Presenter для содержимого -->
        <ContentPresenter x:Name="contentPresenter"
                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                          Margin="{TemplateBinding Padding}"/>
        
        <!-- Иконка (опционально) -->
        <Path x:Name="iconPath"
              Data="{TemplateBinding Tag}"
              Fill="{TemplateBinding Foreground}"
              HorizontalAlignment="Left"
              Margin="10,0,0,0"
              Visibility="Collapsed"/>
    </Grid>
    
    <ControlTemplate.Triggers>
        <!-- IsMouseOver -->
        <Trigger Property="IsMouseOver" Value="True">
            <Trigger.EnterActions>
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetName="backgroundBorder"
                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="#005A9E"
                                        Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </Trigger.EnterActions>
            <Trigger.ExitActions>
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetName="backgroundBorder"
                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="#0078D4"
                                        Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </Trigger.ExitActions>
        </Trigger>
        
        <!-- IsPressed -->
        <Trigger Property="IsPressed" Value="True">
            <Setter TargetName="backgroundBorder" Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" 
                                      ShadowDepth="1"
                                      Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Trigger>
        
        <!-- IsEnabled=False -->
        <Trigger Property="IsEnabled" Value="False">
            <Setter TargetName="backgroundBorder" Property="Opacity" Value="0.5"/>
            <Setter Property="Cursor" Value="Arrow"/>
        </Trigger>
        
        <!-- HasIcon (через Tag) -->
        <DataTrigger Binding="{TemplateBinding Tag}" Value="{x:Null}">
            <Setter TargetName="iconPath" Property="Visibility" Value="Collapsed"/>
        </DataTrigger>
    </ControlTemplate.Triggers>
</ControlTemplate>
```

#### Пример 3: DataTemplate для Person

```xml
<Window.Resources>
    <!-- Простой DataTemplate -->
    <DataTemplate x:Key="PersonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Source="{Binding AvatarUrl}" 
                   Width="40" Height="40"
                   Margin="0,0,10,0"/>
            <StackPanel>
                <TextBlock Text="{Binding Name}" 
                           FontWeight="Bold"
                           FontSize="14"/>
                <TextBlock Text="{Binding Email}" 
                           Foreground="Gray"
                           FontSize="12"/>
            </StackPanel>
        </StackPanel>
    </DataTemplate>
</Window.Resources>

<ListBox ItemsSource="{Binding People}"
         ItemTemplate="{StaticResource PersonTemplate}"/>
```

#### Пример 4: DataTemplate с триггерами

```xml
<DataTemplate x:Key="AdvancedPersonTemplate">
    <Border Padding="10" Margin="5" BorderThickness="1" BorderBrush="#E0E0E0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Аватар -->
            <Ellipse Grid.Column="0" Width="60" Height="60">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                </Ellipse.Fill>
            </Ellipse>
            
            <!-- Информация -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="{Binding Name}" 
                           FontSize="16"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding Position}" 
                           Foreground="Gray"
                           FontSize="13"/>
                <TextBlock Text="{Binding Email}" 
                           Foreground="Gray"
                           FontSize="12"/>
                
                <!-- Статус -->
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Ellipse Width="10" Height="10" Margin="0,0,5,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                        <Setter Property="Fill" Value="#28A745"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsOnline}" Value="False">
                                        <Setter Property="Fill" Value="#6C757D"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsAway}" Value="True">
                                        <Setter Property="Fill" Value="#FFC107"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock FontSize="12">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                        <Setter Property="Text" Value="Онлайн"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsOnline}" Value="False">
                                        <Setter Property="Text" Value="Офлайн"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsAway}" Value="True">
                                        <Setter Property="Text" Value="Отошёл"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</DataTemplate>
```

#### Пример 5: DataTemplateSelector

```csharp
// PersonTemplateSelector.cs
public class PersonTemplateSelector : DataTemplateSelector
{
    public DataTemplate AdminTemplate { get; set; }
    public DataTemplate UserTemplate { get; set; }
    public DataTemplate GuestTemplate { get; set; }

    public override DataTemplate SelectTemplate(object item, DependencyObject container)
    {
        if (item is Person person)
        {
            if (person.IsAdmin)
                return AdminTemplate;
            else if (person.IsPremium)
                return UserTemplate;
            else
                return GuestTemplate;
        }
        
        return base.SelectTemplate(item, container);
    }
}
```

```xml
<Window.Resources>
    <!-- Шаблоны -->
    <DataTemplate x:Key="AdminTemplate">
        <Border Background="#FFF3CD" BorderBrush="#FFC107" BorderThickness="2">
            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
        </Border>
    </DataTemplate>
    
    <DataTemplate x:Key="UserTemplate">
        <Border Background="#D4EDDA" BorderBrush="#28A745" BorderThickness="2">
            <TextBlock Text="{Binding Name}"/>
        </Border>
    </DataTemplate>
    
    <DataTemplate x:Key="GuestTemplate">
        <Border Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="1">
            <TextBlock Text="{Binding Name}" Foreground="Gray"/>
        </Border>
    </DataTemplate>
    
    <!-- Селектор -->
    <local:PersonTemplateSelector x:Key="PersonTemplateSelector"
                                   AdminTemplate="{StaticResource AdminTemplate}"
                                   UserTemplate="{StaticResource UserTemplate}"
                                   GuestTemplate="{StaticResource GuestTemplate}"/>
</Window.Resources>

<ListBox ItemsSource="{Binding People}"
         ItemTemplateSelector="{StaticResource PersonTemplateSelector}"/>
```

#### Пример 6: ItemsPanelTemplate

```xml
<ListBox ItemsSource="{Binding Items}">
    <ListBox.ItemsPanel>
        <ItemsPanelTemplate>
            <WrapPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>
    </ListBox.ItemsPanel>
    
    <ListBox.ItemTemplate>
        <DataTemplate>
            <Border Background="LightBlue" 
                    BorderBrush="Blue"
                    BorderThickness="1"
                    Margin="5"
                    Padding="20,10">
                <TextBlock Text="{Binding Name}"/>
            </Border>
        </DataTemplate>
    </ListBox.ItemTemplate>
</ListBox>
```

#### Пример 7: ControlTemplate для ComboBox (упрощённо)

```xml
<ControlTemplate TargetType="ComboBox">
    <Grid>
        <ToggleButton x:Name="ToggleButton"
                      Focusable="False"
                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                      ClickMode="Press">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            
                            <ContentPresenter Grid.Column="0"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              VerticalAlignment="Center"
                                              Margin="5"/>
                            
                            <Path Grid.Column="1"
                                  Data="M0,0 L5,5 L10,0 Z"
                                  Fill="Gray"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
        
        <Popup x:Name="Popup"
               IsOpen="{TemplateBinding IsDropDownOpen}"
               Placement="Bottom"
               Focusable="False"
               AllowsTransparency="True">
            <Border Background="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    MaxHeight="300">
                <ScrollViewer>
                    <ItemsPresenter/>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</ControlTemplate>
```

#### Пример 8: Реальный DataTemplate из DotElectric

```xml
<!-- EditorCanvas.xaml — шаблон для Line -->
<UserControl.Resources>
    <DataTemplate DataType="{x:Type models:Line}">
        <Line Stroke="Black" StrokeThickness="1">
            <Line.Style>
                <Style TargetType="Line">
                    <Setter Property="X1">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource LocalX1Converter}">
                                <Binding Path="StartMicronsX"/>
                                <Binding Path="EndMicronsX"/>
                                <Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Y1">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource LocalY1Converter}">
                                <Binding Path="StartMicronsY"/>
                                <Binding Path="EndMicronsY"/>
                                <Binding Path="DataContext.Template.Sheet.HeightMm" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                <Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="X2">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource LocalX2Converter}">
                                <Binding Path="StartMicronsX"/>
                                <Binding Path="EndMicronsX"/>
                                <Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Y2">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource LocalY2Converter}">
                                <Binding Path="StartMicronsY"/>
                                <Binding Path="EndMicronsY"/>
                                <Binding Path="DataContext.Template.Sheet.HeightMm" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                <Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="StrokeDashArray" 
                            Value="{Binding LineType, Converter={StaticResource LineTypeToDashArrayConverter}}"/>
                    
                    <!-- Hover эффект -->
                    <Style.Triggers>
                        <DataTrigger Value="True">
                            <DataTrigger.Binding>
                                <MultiBinding Converter="{StaticResource IsHoveredConverter}">
                                    <Binding/>
                                    <Binding Path="DataContext.HoveredObject" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                </MultiBinding>
                            </DataTrigger.Binding>
                            <Setter Property="StrokeThickness" Value="2.5"/>
                            <Setter Property="Stroke" Value="#333"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Line.Style>
        </Line>
    </DataTemplate>
    
    <!-- Шаблон для Rectangle -->
    <DataTemplate DataType="{x:Type models:Rectangle}">
        <Rectangle Stroke="Black" Fill="Transparent" StrokeThickness="1">
            <Rectangle.Width>
                <MultiBinding Converter="{StaticResource MicronsToPixelConverter}">
                    <Binding Path="WidthMicrons"/>
                    <Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                </MultiBinding>
            </Rectangle.Width>
            <Rectangle.Height>
                <MultiBinding Converter="{StaticResource MicronsToPixelConverter}">
                    <Binding Path="HeightMicrons"/>
                    <Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                </MultiBinding>
            </Rectangle.Height>
            <Rectangle.Style>
                <Style TargetType="Rectangle">
                    <Setter Property="StrokeDashArray" 
                            Value="{Binding LineType, Converter={StaticResource LineTypeToDashArrayConverter}}"/>
                </Style>
            </Rectangle.Style>
        </Rectangle>
    </DataTemplate>
    
    <!-- Шаблон для Text -->
    <DataTemplate DataType="{x:Type models:Text}">
        <TextBlock Foreground="Black" FontSize="12">
            <TextBlock.Text>
                <Binding Path="Content"/>
            </TextBlock.Text>
            <TextBlock.LayoutTransform>
                <RotateTransform>
                    <RotateTransform.Angle>
                        <Binding Path="RotationAngle"/>
                    </RotateTransform.Angle>
                </RotateTransform>
            </TextBlock.LayoutTransform>
        </TextBlock>
    </DataTemplate>
</UserControl.Resources>
```

---

### Задачи

#### 🟢 Базовый уровень (45 минут)

**Задача 2.5.1: Простой ControlTemplate**

Создайте ControlTemplate для Button:
- Border с закруглёнными углами (CornerRadius=10)
- ContentPresenter по центру
- Trigger на IsMouseOver (изменение цвета фона)

**Задача 2.5.2: DataTemplate для книги**

Создайте DataTemplate для класса `Book`:
- Название (жирный шрифт)
- Автор (серый текст)
- Год издания
- Обложка (Image)

---

#### 🟡 Средний уровень (1.5 часа)

**Задача 2.5.3: ControlTemplate для ToggleButton**

Создайте ControlTemplate для ToggleButton:
- Визуальное переключение (вкл/выкл)
- Анимация перехода (ColorAnimation)
- Индикатор состояния (галочка или круг)

**Задача 2.5.4: DataTemplateSelector для сообщений**

Создайте `MessageTemplateSelector`:
- Входящие: синий фон
- Исходящие: зелёный фон
- Системные: серый фон
- Реализуйте через DataTemplateSelector

---

#### 🔴 Продвинутый уровень (3 часа)

**Задача 2.5.5: Полный ControlTemplate для TextBox**

Создайте полный ControlTemplate для TextBox:
- PART_ContentHost (обязательная часть)
- Border с анимацией фокуса
- Placeholder текст (через Attached Property)
- Кнопка очистки (видна при вводе текста)

**Задача 2.5.6: Карусель с ItemsPanelTemplate**

Создайте ItemsControl с кастомной панелью:
- Горизонтальная прокрутка
- Эффект карусели (центральный элемент крупнее)
- DataTemplate с карточками
- Анимация при прокрутке

---

### Решения

<details>
<summary>✅ Решение задачи 2.5.1</summary>

```xml
<Window.Resources>
    <ControlTemplate x:Key="RoundedButtonTemplate" TargetType="Button">
        <Border x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="10">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"/>
        </Border>
        
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</Window.Resources>

<Button Content="Rounded Button"
        Background="#0078D4"
        BorderBrush="#005A9E"
        BorderThickness="2"
        Padding="20,10"
        Template="{StaticResource RoundedButtonTemplate}"/>
```
</details>

<details>
<summary>✅ Решение задачи 2.5.2</summary>

```xml
<DataTemplate x:Key="BookTemplate">
    <Border Padding="10" Margin="5" BorderThickness="1" BorderBrush="#E0E0E0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Обложка -->
            <Image Grid.Column="0" 
                   Source="{Binding CoverUrl}"
                   Width="60" Height="80"
                   Stretch="UniformToFill"/>
            
            <!-- Информация -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="{Binding Title}" 
                           FontWeight="Bold"
                           FontSize="16"
                           TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Author}" 
                           Foreground="Gray"
                           FontSize="14"
                           Margin="0,5,0,0"/>
                <TextBlock FontSize="12"
                           Foreground="LightGray"
                           Margin="0,5,0,0">
                    <Run Text="Год издания: "/>
                    <Run Text="{Binding Year}"/>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</DataTemplate>
```
</details>

<details>
<summary>✅ Решение задачи 2.5.3</summary>

```xml
<ControlTemplate TargetType="ToggleButton">
    <Grid>
        <Border x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="15"
                Width="60" Height="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Opacity="0.3"/>
            </Border.Effect>
        </Border>
        
        <!-- Индикатор -->
        <Ellipse x:Name="indicator"
                 Width="24" Height="24"
                 Fill="White"
                 HorizontalAlignment="Left"
                 Margin="3,0,0,0">
            <Ellipse.RenderTransform>
                <TranslateTransform X="0"/>
            </Ellipse.RenderTransform>
        </Ellipse>
    </Grid>
    
    <ControlTemplate.Triggers>
        <Trigger Property="IsChecked" Value="True">
            <Setter TargetName="border" Property="Background" Value="#28A745"/>
            <Setter TargetName="indicator" Property="HorizontalAlignment" Value="Right"/>
            <Setter TargetName="indicator" Property="Margin" Value="0,0,3,0"/>
        </Trigger>
        
        <EventTrigger RoutedEvent="Checked">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="indicator"
                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                     To="30"
                                     Duration="0:0:0.2"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
        
        <EventTrigger RoutedEvent="Unchecked">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="indicator"
                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                     To="0"
                                     Duration="0:0:0.2"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </ControlTemplate.Triggers>
</ControlTemplate>
```
</details>

---

## Ключевые выводы

✅ **ControlTemplate** определяет визуальную структуру контрола  
✅ **DataTemplate** определяет как отображаются данные  
✅ **TemplateBinding** передаёт свойства из контрола в template  
✅ **TemplatePart** — именованные части шаблона (соглашение PART_)  
✅ **DataTemplateSelector** — выбор шаблона в runtime по логике  
✅ **ItemsPanelTemplate** — панель для ItemsControl  
✅ **ContentPresenter** — отображает Content контрола в template

---

## Дополнительные ресурсы

- [Control Templating](https://docs.microsoft.com/en-us/dotnet/desktop/wpf/fundamentals/control-templating)
- [Data Templating](https://docs.microsoft.com/en-us/dotnet/desktop/wpf/data/data-templating-overview)
- [Template Parts](https://docs.microsoft.com/en-us/dotnet/api/system.windows.templatepartattribute)
