# Модуль 11: Практическая работа

## Задание: Тестирование MVVM приложения

**Время выполнения:** 4 часа  
**Уровень:** Продвинутый  
**Максимальный балл:** 100

---

## Описание

Необходимо написать комплексные тесты для WPF приложения: unit-тесты ViewModel, мокирование сервисов, тесты команд и UI тесты.

---

## Требования

### 1. Настройка тестового проекта (30 минут)

Создайте тестовый проект:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="xunit" Version="2.9.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.0" />
    <PackageReference Include="Moq" Version="4.20.72" />
    <PackageReference Include="FlaUI.UIA3" Version="4.0.0" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\DotElectric.TemplateEditor\DotElectric.TemplateEditor.csproj" />
  </ItemGroup>
</Project>
```

---

### 2. ViewModel тесты (1 час)

Напишите тесты для ViewModel:

```csharp
using Xunit;
using DotElectric.ViewModels;

public class MainViewModelTests
{
    [Fact]
    public void Title_Get_ReturnsDefaultValue()
    {
        // Arrange
        var vm = new MainViewModel();
        
        // Act
        var result = vm.Title;
        
        // Assert
        Assert.Equal("Untitled", result);
    }
    
    [Fact]
    public void Zoom_Setter_ShouldRaisePropertyChanged()
    {
        // Arrange
        var vm = new MainViewModel();
        var raised = false;
        
        vm.PropertyChanged += (s, e) =>
        {
            if (e.PropertyName == nameof(MainViewModel.Zoom))
                raised = true;
        };
        
        // Act
        vm.Zoom = 1.5;
        
        // Assert
        Assert.True(raised);
    }
    
    [Fact]
    public void OpenedTabs_Add_ShouldRaiseCollectionChanged()
    {
        // Arrange
        var vm = new MainViewModel();
        var raised = false;
        
        vm.OpenedTabs.CollectionChanged += (s, e) => raised = true;
        
        // Act
        vm.OpenedTabs.Add(new EditorViewModel());
        
        // Assert
        Assert.True(raised);
    }
}
```

---

### 3. Мокирование сервисов (45 минут)

Напишите тесты с Moq:

```csharp
using Xunit;
using Moq;
using DotElectric.ViewModels;
using DotElectric.Services;

public class EditorViewModelTests
{
    [Fact]
    public void NewTemplate_ShouldCallTemplateService()
    {
        // Arrange
        var mockTemplateService = new Mock<ITemplateService>();
        var newTemplate = new Template();
        mockTemplateService
            .Setup(x => x.CreateNew())
            .Returns(newTemplate);
        
        var vm = new EditorViewModel(mockTemplateService.Object);
        
        // Act
        vm.NewTemplateCommand.Execute(null);
        
        // Assert
        mockTemplateService.Verify(x => x.CreateNew(), Times.Once);
        Assert.Same(newTemplate, vm.Template);
    }
    
    [Fact]
    public async Task Save_WithValidPath_ShouldCallFileService()
    {
        // Arrange
        var mockTemplateService = new Mock<ITemplateService>();
        var mockDialogService = new Mock<IDialogService>();
        var mockFileService = new Mock<IFileService>();
        
        mockDialogService
            .Setup(x => x.ShowSaveFileDialog(It.IsAny<string>()))
            .Returns("C:\\test.tdel");
        
        mockFileService
            .Setup(x => x.SaveAsync(It.IsAny<Template>(), "C:\\test.tdel"))
            .Returns(Task.CompletedTask);
        
        var vm = new EditorViewModel(
            mockTemplateService.Object,
            mockDialogService.Object,
            mockFileService.Object);
        
        // Act
        await vm.SaveCommand.ExecuteAsync(null);
        
        // Assert
        mockFileService.Verify(
            x => x.SaveAsync(It.IsAny<Template>(), "C:\\test.tdel"), 
            Times.Once);
    }
}
```

---

### 4. Command тесты (45 минут)

Напишите тесты для команд:

```csharp
using Xunit;
using Moq;
using DotElectric.ViewModels;
using DotElectric.Services;

public class CommandTests
{
    [Fact]
    public void ZoomInCommand_Execute_ShouldIncreaseZoom()
    {
        // Arrange
        var mockTemplateService = new Mock<ITemplateService>();
        var vm = new EditorViewModel(mockTemplateService.Object);
        var initialZoom = vm.Zoom;
        
        // Act
        vm.ZoomInCommand.Execute(null);
        
        // Assert
        Assert.True(vm.Zoom > initialZoom);
    }
    
    [Fact]
    public void UndoCommand_CanExecute_WhenHistoryHasItems()
    {
        // Arrange
        var mockTemplateService = new Mock<ITemplateService>();
        var vm = new EditorViewModel(mockTemplateService.Object);
        
        // Add item to history
        // ...
        
        // Act & Assert
        Assert.True(vm.UndoCommand.CanExecute(null));
    }
    
    [Fact]
    public void ToggleGridCommand_Execute_ShouldToggleValue()
    {
        // Arrange
        var mockTemplateService = new Mock<ITemplateService>();
        var vm = new EditorViewModel(mockTemplateService.Object);
        var initialState = vm.StatusBarGridEnabled;
        
        // Act
        vm.ToggleGridCommand.Execute(null);
        
        // Assert
        Assert.NotEqual(initialState, vm.StatusBarGridEnabled);
    }
}
```

---

### 5. UI тесты с FlaUI (1 час)

Напишите UI тесты:

```csharp
using Xunit;
using FlaUI.UIA3;
using FlaUI.Core.AutomationElements;

public class MainWindowTests
{
    [Fact]
    public void MainWindow_ShouldOpen()
    {
        // Arrange
        var app = Application.Launch("DotElectric.TemplateEditor.exe");
        
        using (var automation = new UIA3Automation())
        {
            var window = app.GetMainWindow(automation);
            window.WaitForIdle(TimeSpan.FromSeconds(5));
            
            // Assert
            Assert.NotNull(window);
            Assert.Contains("DotElectric", window.Name);
        }
        
        app.Close();
    }
    
    [Fact]
    public void NewTemplateButton_Click_ShouldCreateNewTab()
    {
        // Arrange
        var app = Application.Launch("DotElectric.TemplateEditor.exe");
        
        using (var automation = new UIA3Automation())
        {
            var window = app.GetMainWindow(automation);
            window.WaitForIdle(TimeSpan.FromSeconds(5));
            
            // Act
            var newButton = window.FindFirstDescendant(
                cf => cf.ByAutomationId("NewTemplateButton"));
            newButton?.Click();
            
            window.WaitForIdle(TimeSpan.FromSeconds(2));
            
            // Assert
            var tabControl = window.FindFirstDescendant(
                cf => cf.ByAutomationId("MainTabControl"));
            var tabs = tabControl.FindAllDescendants(
                cf => cf.ControlType == FlaUI.Core.Definitions.ControlType.TabItem);
            
            Assert.NotEmpty(tabs);
        }
        
        app.Close();
    }
    
    [Fact]
    public void ZoomComboBox_Change_ShouldUpdateZoom()
    {
        // Arrange
        var app = Application.Launch("DotElectric.TemplateEditor.exe");
        
        using (var automation = new UIA3Automation())
        {
            var window = app.GetMainWindow(automation);
            window.WaitForIdle(TimeSpan.FromSeconds(5));
            
            // Act
            var zoomComboBox = window.FindFirstDescendant(
                cf => cf.ByAutomationId("ZoomComboBox"));
            zoomComboBox.AsComboBox().Select("200%");
            
            window.WaitForIdle(TimeSpan.FromSeconds(1));
            
            // Assert
            var zoomText = window.FindFirstDescendant(
                cf => cf.ByAutomationId("ZoomTextBlock"));
            Assert.Contains("200%", zoomText.Name);
        }
        
        app.Close();
    }
}
```

---

## Критерии оценки

| Критерий | Баллы |
|----------|-------|
| **ViewModel тесты** | 25 |
| — Property тесты | 8 |
| — PropertyChanged тесты | 8 |
| — Collection тесты | 9 |
| **Moq тесты** | 25 |
| — Mock сервиса | 8 |
| — Setup с Returns | 8 |
| — Verify вызовов | 9 |
| **Command тесты** | 25 |
| — CanExecute тесты | 8 |
| — Execute тесты | 8 |
| — Async Command тесты | 9 |
| **UI тесты** | 20 |
| — Launch App | 5 |
| — Find Element | 5 |
| — Click и Verify | 5 |
| — Page Object (опционально) | 5 |
| **Code Quality** | 5 |
| — Именование тестов | 2 |
| — Arrange-Act-Assert | 2 |
| — Комментарии | 1 |
| **Итого** | **100** |

---

## Дополнительные задания (со звёздочкой)

### ⭐ Code Coverage (30 минут)

Запустите code coverage:
- dotnet test /p:CollectCoverage=true
- 80%+ coverage
- Отчёт в HTML

**Бонус:** +10 баллов

### ⭐⭐ Integration Tests (1 час)

Напишите интеграционные тесты:
- Несколько ViewModel вместе
- Реальные сервисы (не mock)
- Состояние после действий

**Бонус:** +15 баллов

### ⭐⭐⭐ CI/CD Integration (2 часа)

Настройте CI/CD:
- GitHub Actions workflow
- Запуск тестов при push
- Отчёт о coverage

**Бонус:** +20 баллов

---

## Чек-лист перед сдачей

- [ ] xUnit проект настроен
- [ ] 10+ ViewModel тестов
- [ ] 5+ Moq тестов
- [ ] 5+ Command тестов
- [ ] 3+ UI теста
- [ ] Все тесты проходят
- [ ] Code coverage 80%+
- [ ] Код компилируется без ошибок

---

## Ресурсы

- [xUnit Documentation](https://xunit.net/docs)
- [Moq Documentation](https://github.com/moq/moq4)
- [FlaUI Documentation](https://github.com/FlaUI/FlaUI)
- [Code Coverage](https://docs.microsoft.com/en-us/visualstudio/test/using-code-coverage-to-determine-how-much-code-is-being-tested)
