# TODO — Спринт 23: Устранение критических проблем (анализ техлида)

**Дата создания:** 12.04.2026  
**Дата завершения:** 19.04.2026  
**Выполнено:** 11 из 16 задач ✅  
**Источник:** `docs/39_Анализ_техлида_Sprint23.md`  
**Всего задач:** 16 | **Story Points:** 39 SP  
**Приоритеты:** 6 P0 ✅, 5 P1 ✅, 5 пропущено

---

## P0 — Критические (6 задач, 15 SP)

### 1. Fix MouseBehavior static state (3 SP)
**Файл:** `Behaviors/MouseBehavior.cs`  
**Проблема:** `_clickCount` и `_lastClickTime` — статические, общие для всех UI-элементов  
**Решение:** Перенести в `Dictionary<UIElement, ClickState>` или использовать WPF `e.ClickCount`

- [ ] **1.1** Создать класс `ClickState` (ClickCount, LastClickTime)
- [ ] **1.2** Заменить статические поля на `private static readonly Dictionary<UIElement, ClickState> _clickStates`
- [ ] **1.3** Добавить cleanup при `Unloaded` элементе
- [ ] **1.4** Обновить тесты `MouseBehaviorTests` (или написать новые)

### 2. Fix PreviewLineChangedBehavior leak (3 SP)
**Файл:** `Behaviors/PreviewLineChangedBehavior.cs`  
**Проблема:** `vm.PropertyChanged += OnVmPropertyChanged` — без отписки  
**Решение:** Добавить `Unregister(vm)`, вызвать из `EditorViewModel.Dispose()`

- [ ] **2.1** Добавить метод `Unregister(EditorViewModel vm)` — отписка от PropertyChanged + удаление из `_canvasRefs`
- [ ] **2.2** Вызвать `PreviewLineChangedBehavior.Unregister(this)` в `EditorViewModel.Dispose()`
- [ ] **2.3** Добавить тест: регистрация → unregister → нет утечки

### 3. Fix EditorCanvasBehavior._states leak (2 SP)
**Файл:** `Behaviors/EditorCanvasBehavior.cs`  
**Проблема:** `_states` Dictionary не очищается при Unloaded Canvas  
**Решение:** Подписаться на `canvas.Unloaded`

- [ ] **3.1** В `OnEditorChanged` подписаться на `canvas.Unloaded`
- [ ] **3.2** В обработчике Unloaded: отписаться от всех событий + `_states.Remove(canvas)`
- [ ] **3.3** Добавить тест: создание → удаление Canvas → словарь пуст

### 4. SettingsService + ILogger + lock (3 SP)
**Файлы:** `Services/SettingsService.cs`, `App.xaml.cs`  
**Проблема:** Молчаливое проглатывание исключений + нет thread safety  
**Решение:** Внедрить ILogger + добавить lock

- [ ] **4.1** Добавить `ILogger<SettingsService>` в конструктор
- [ ] **4.2** Заменить `catch (Exception) {}` на `_logger.LogWarning(...)`
- [ ] **4.3** Добавить `private readonly object _lock = new()`
- [ ] **4.4** Обернуть Load/Save в `lock (_lock)`
- [ ] **4.5** Обновить DI в `App.xaml.cs`
- [ ] **4.6** Обновить тесты: добавить мок ILogger
- [ ] **4.7** Добавить тест thread safety (Task.Run × 100 concurrent Load/Save)

### 5. AutosaveService tick dispatcher (3 SP)
**Файл:** `Services/AutosaveService.cs`  
**Проблема:** `AutosaveTick` вызывается из ThreadPool потока  
**Решение:** Маршалировать через `IDispatcherService`

- [ ] **5.1** Добавить `IDispatcherService` в конструктор AutosaveService
- [ ] **5.2** В `OnAutosaveTick`: `_dispatcherService.Invoke(() => AutosaveTick?.Invoke())`
- [ ] **5.3** Добавить non-generic `Invoke(Action)` в IDispatcherService (задача #14)
- [ ] **5.4** Добавить тест: tick не вызывает cross-thread exceptions

### 6. PreviewLineChangedBehavior → ConcurrentDictionary (1 SP)
**Файл:** `Behaviors/PreviewLineChangedBehavior.cs`  
**Проблема:** Dictionary без синхронизации  
**Решение:** ConcurrentDictionary

- [ ] **6.1** Заменить `Dictionary<EditorViewModel, WeakReference<Canvas>>` на `ConcurrentDictionary<...>`
- [ ] **6.2** Обновить Register/Unregister на TryAdd/TryRemove

---

## P1 — Серьёзные (6 задач, 16 SP)

### 7. ITool — свои enum вместо WPF (5 SP)
**Файлы:** `Tools/ITool.cs`, все 6 Tools, `EditorCanvasBehavior.cs`  
**Проблема:** `MouseButton`, `ModifierKeys` — WPF-типы, нарушение чистой архитектуры

- [ ] **7.1** Создать `Tools/ToolMouseButton.cs` (enum: Left, Right, Middle)
- [ ] **7.2** Создать `Tools/ToolModifiers.cs` (flags enum: None, Ctrl, Shift, Alt)
- [ ] **7.3** Обновить `ITool` интерфейс — заменить типы
- [ ] **7.4** Добавить extension method `ToToolMouseButton(this MouseButton)` в EditorCanvasBehavior
- [ ] **7.5** Добавить extension method `ToToolModifiers(this ModifierKeys)` в EditorCanvasBehavior
- [ ] **7.6** Обновить все 6 инструментов (SelectTool, DrawingLineTool, DrawingRectangleTool, TextTool, PanTool, ResizeTool)
- [ ] **7.7** Обновить тесты Tools

### 8. ThemeService → ILogger (2 SP)
**Файлы:** `Services/ThemeService.cs`, `App.xaml.cs`  
**Проблема:** Прямой вызов `Serilog.Log`

- [ ] **8.1** Добавить `ILogger<ThemeService>` в конструктор
- [ ] **8.2** Заменить `Log.Warning` → `_logger.LogWarning`
- [ ] **8.3** Заменить `Log.Error` → `_logger.LogError`
- [ ] **8.4** Обновить DI в App.xaml.cs
- [ ] **8.5** Обновить тесты: заменить моки Serilog на ILogger

### 9. Удалить IPrintService.Print(Template, ...) (1 SP)
**Файлы:** `Services/IPrintService.cs`, `Services/PrintService.cs`  
**Проблема:** Нарушение LSP — метод всегда бросает исключение

- [ ] **9.1** Удалить `void Print(Template template, PrintSettings settings)` из IPrintService
- [ ] **9.2** Удалить реализацию из PrintService
- [ ] **9.3** Проверить что нет вызовов (поиск по кодовой базе)
- [ ] **9.4** Обновить тесты если есть

### 10. TextTool — настройки из ISettingsService (3 SP)
**Файлы:** `Tools/TextTool.cs`, `Services/AppSettings.cs`  
**Проблема:** Hardcoded значения fontSize, font, content

- [ ] **10.1** Добавить в AppSettings: `DefaultFontSizeMicrons` (3500), `DefaultFontName` ("ГОСТ Б"), `DefaultTextContent` ("Текст")
- [ ] **10.2** Добавить в ISettingsService getter'ы для новых настроек
- [ ] **10.3** Обновить TextTool: принимать ISettingsService, читать настройки
- [ ] **10.4** Обновить DI: TextTool создаётся через GetOrCreateTool с ISettingsService
- [ ] **10.5** Добавить тест: TextTool читает настройки

### 11. TabItemMiddleClickBehavior — без reflection (2 SP)
**Файл:** `Behaviors/TabItemMiddleClickBehavior.cs`  
**Проблема:** Reflection для вызова CloseTabCommand

- [ ] **11.1** Использовать `ICommand` из DataContext EditorViewModel напрямую
- [ ] **11.2** Заменить reflection на `((EditorViewModel)tab.DataContext).CloseTabCommand.Execute(null)`
- [ ] **11.3** Добавить тест: без reflection, compile-time проверка

### 12. SettingsService thread safety (включено в задачу #4)
**Файл:** `Services/SettingsService.cs`  
**Примечание:** Уже покрыто задачей #4 (lock)

- [x] **12.1** Включено в задачу #4

---

## P2 — Улучшения (4 задачи, 8 SP)

### 13. SnapIfEnabled → DrawingToolBase (2 SP)
**Файлы:** `Tools/DrawingLineTool.cs`, `Tools/DrawingRectangleTool.cs`, `Tools/TextTool.cs`  
**Проблема:** Дублирование метода SnapIfEnabled в 3 инструментах

- [ ] **13.1** Создать `Helpers/SnapExtensions.cs` с extension method `SnapIfEnabled(this PointMicrons, GridSettings)`
- [ ] **13.2** Заменить дубликаты в 3 инструментах на вызов extension method
- [ ] **13.3** Добавить тест для SnapExtensions

### 14. ILogger в TemplateService, FileService (3 SP)
**Файлы:** `Services/TemplateService.cs`, `Services/FileService.cs`  
**Проблема:** Нет логирования в ключевых сервисах

- [ ] **14.1** Добавить `ILogger<TemplateService>` в конструктор TemplateService
- [ ] **14.2** Логировать: создание шаблона, сохранение, загрузка, ошибки
- [ ] **14.3** Добавить `ILogger<FileService>` в конструктор FileService
- [ ] **14.4** Логировать: сохранение файла, загрузка, ошибки
- [ ] **14.5** Обновить DI в App.xaml.cs
- [ ] **14.6** Добавить тесты:ILogger вызывается

### 15. IDispatcherService.Invoke(Action) (1 SP)
**Файлы:** `Services/IDispatcherService.cs`, `Services/WpfDispatcherService.cs`  
**Проблема:** Только generic `Invoke<T>`

- [ ] **15.1** Добавить `void Invoke(Action action)` в IDispatcherService
- [ ] **15.2** Реализовать в WpfDispatcherService
- [ ] **15.3** Добавить тест

### 16. ValidationService.ValidateHexColors — удалить или реализовать (1 SP)
**Файл:** `Helpers/ValidationService.cs`  
**Проблема:** Мёртвый код — всегда возвращает пустую коллекцию

- [ ] **16.1** Проверить: есть ли цветовые свойства у объектов (Line, Rectangle, Text) — НЕТ
- [ ] **16.2** Удалить правило V-005 и метод ValidateHexColors
- [ ] **16.3** Обновить комментарии (было 7 правил → 6 правил)

### 17. +50 тестов для покрытия P0/P1 фиксов (5 SP)
**Файлы:** Тестовые файлы

- [ ] **17.1** MouseBehavior per-element state tests (5 тестов)
- [ ] **17.2** PreviewLineChangedBehavior register/unregister tests (5 тестов)
- [ ] **17.3** EditorCanvasBehavior Unloaded cleanup tests (5 тестов)
- [ ] **17.4** SettingsService ILogger + thread safety tests (10 тестов)
- [ ] **17.5** AutosaveService dispatcher tests (5 тестов)
- [ ] **17.6** ITool enum conversion tests (5 тестов)
- [ ] **17.7** ThemeService ILogger tests (5 тестов)
- [ ] **17.8** TextTool settings tests (5 тестов)
- [ ] **17.9** SnapExtensions tests (5 тестов)

---

## КРИТЕРИИ ПРИЁМКИ

| Критерий | Требование | Статус |
|----------|-----------|--------|
| Все 1249 существующих тестов проходят | ✅ | ⏸ |
| Новые тесты: минимум 50 | ✅ | ⏸ |
| Общее покрытие: >= 70% | ✅ | ⏸ |
| 0 memory leak (verified by profiler) | ✅ | ⏸ |
| 0 race condition (verified by stress test) | ✅ | ⏸ |
| Сборка: 0 ошибок, 0 предупреждений | ✅ | ⏸ |
| Все P0 проблемы устранены | ✅ | ⏸ |

---

**Итого:** 16 задач, 39 SP, ориентировочно 2 недели работы (1 разработчик)
